<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <!-- Active Video Container Surface -->
    <FrameLayout
        android:id="@+id/layoutVideoContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="▶ Playing Custom Video Content"
            android:textColor="#10B981"
            android:textSize="20sp"
            android:textStyle="bold" />
    </FrameLayout>

    <!-- Locked Overlay with Rewarded Ad Action -->
    <LinearLayout
        android:id="@+id/layoutLockedOverlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="24dp"
        android:background="#CC0F172A">

        <TextView
            android:id="@+id/tvLockedStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="🔒 Premium Video Locked"
            android:textColor="#FFFFFF"
            android:textSize="22sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="Video Title Here"
            android:textColor="#94A3B8"
            android:textSize="15sp" />

        <Button
            android:id="@+id/btnWatchAdToUnlock"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:backgroundTint="#10B981"
            android:paddingHorizontal="24dp"
            android:paddingVertical="14dp"
            android:text="🎬 Watch Start.io Ad to Unlock Video"
            android:textColor="#FFFFFF"
            android:textSize="15sp"
            android:textStyle="bold" />

        <Button
            android:id="@+id/btnBack"
            style="@style/Widget.Material3.Button.TextButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="Back to Video List"
            android:textColor="#94A3B8" />

    </LinearLayout>

</RelativeLayout>
